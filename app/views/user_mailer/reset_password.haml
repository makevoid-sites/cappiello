- url = reset_password_url(@user.reset_password_token)
- link = link_to url, url

%p
  - if @user.en?
    Follow this link to reset your password: #{link}
  - else
    Clicca qui per effettuare il ripristino della password: #{link}